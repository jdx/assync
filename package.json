{
  "name": "assync",
  "description": "async array helpers",
  "version": "0.0.0",
  "author": "Jeff Dickey @jdxcode",
  "devDependencies": {
    "del-cli": "^1.1.0",
    "husky": "^0.14.3",
    "jest": "^22.0.3",
    "lint-staged": "^6.0.0",
    "prettier": "^1.9.2",
    "ts-jest": "^22.0.0",
    "tslint": "^5.8.0",
    "tslint-cli-engine": "^1.0.2",
    "typescript": "^2.6.2"
  },
  "files": [
    "lib"
  ],
  "license": "ISC",
  "main": "lib/assync.js",
  "repository": "jdxcode/assync",
  "scripts": {
    "posttest": "tslint -p . && prettier -l 'src/**/*.ts'",
    "precommit": "lint-staged",
    "prepare": "del-cli lib && tsc && del-cli 'lib/**/*.test.d+(d.ts|js)'",
    "pretest": "tsc",
    "prune": "del-cli \"lib/**/*\\.test\\.(d.ts|js)\"",
    "test": "jest"
  }
}
